"""Bundled demo datasets — shipped inside the wheel.

These exist so a first-time evaluator can ``pip install sablier-flow`` and
have a working verdict in under sixty seconds, without yfinance, an API
key for a market-data vendor, or even internet access. Not intended for
production backtests — they're frozen historical slices used purely for
trying the SDK.
"""
